/**
 * Metro resuelve `.native` / `.web` en tiempo de build.
 * Este archivo satisface la resolución de módulos de TypeScript.
 */
export { LocationPicker } from './LocationPicker.native';
export type { LocationPickerProps } from './LocationPicker.native';
